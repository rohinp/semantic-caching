# Kotlin Semantic Cache

A deterministic, in-memory graph-backed semantic cache. Embedding and LLM
services are injected, so the example needs no network access, credentials, or
external infrastructure.

## Run

```bash
./gradlew test --rerun-tasks
./gradlew run
```
